# letの場合
## 表示予想
0-9が順番に表示される。
forループ外のiはエラーになる。

## 表示結果
0-9が順番に表示され、forループ外のiはエラーになった。
iはforループ内がブロックスコープであり、forループ外から参照することができないため。

# コード内の全ての let を var に変えた場合
## 結果
0-10が順番に表示された。
## 理由
varで定義すると関数スコープとなり、forループ外から参照することが可能となったため。

# 全ての let を消した場合
非staticモードにしてもエラーとなったため実行できず



